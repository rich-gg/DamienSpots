# Handle line endings automatically for text files
* text=auto

# Explicitly mark files that should always have LF line endings
*.sh text eol=lf
*.gradle text eol=lf

# Explicitly mark files that should always have CRLF line endings
*.bat text eol=crlf

# Binary files should not be modified
*.png binary
*.jpg binary
*.gif binary
